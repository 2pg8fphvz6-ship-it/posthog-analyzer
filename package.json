{
  "name": "posthog-analyzer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd dashboard && npm install && npm run build",
    "collect": "tsx src/index.ts collect",
    "score": "tsx src/index.ts score",
    "deploy": "npm run score && git add data/scores.json && git commit -m 'chore: update scores' && git push"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
